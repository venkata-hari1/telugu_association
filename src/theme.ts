import { createTheme } from '@mui/material/styles';
const theme = createTheme({
    palette: {
        background: {
          default:'#FDF7E1', 
        },
    },
    typography: {
      fontFamily: 'Roboto'
    },
    
  });
export default theme
  